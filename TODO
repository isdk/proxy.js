重构：
  ✔ 梳理 ProxySiteConfig 重构方案：统一 Gatekeeping 与 Fingerprinting 配置 [2024-05-11]
    - 统一使用 ProxyMatchPatterns 处理路径与字段匹配
    - 实现双遍扫描流水线，配置语义正交化
  ✔ 梳理规则默认行为：headers/cookies 默认拒绝，query 默认允许 [2024-05-11]
  ☐ 将 body 文本 content-type 判断逻辑（isCacheable.ts）提取为可配置常量，支持用户自定义 @low

测试：
  ✔ 全量迁移测试用例至 V8 架构 [2024-05-11]
  ✔ 增加组合 MatchPatterns (['*', '!id']) 深度验证测试 [2024-05-11]
  ☐ SmartCache 流式错误回滚测试：验证 pipeline 失败时缓存元数据是否正确清理
  ☐ fetchWithCache 304 处理：验证 Response.body 确实为 null
  ☐ prefetch 资源释放：验证 AbortSignal 取消后 activeCacheWrites 正确清理，防止内存泄漏
  ⚠️ 路径前缀匹配边缘情况：/api 匹配 /api/v1 (✓) 但可能误匹配 /api-old (需评估是否需要精确边界)
